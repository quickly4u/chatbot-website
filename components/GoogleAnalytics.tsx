// This component is now a no-op as we're using GTM for Google Analytics
// Keeping it as a placeholder in case we need to add any GTM-specific GA4 configuration
export default function GoogleAnalytics() {
  return null
}
